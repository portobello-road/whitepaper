% use the amsbook style, default to 10pt font and use double sided formatting
% options
\documentclass[twoside]{amsbook}
\numberwithin{figure}{chapter}

\usepackage{euler}

% Set syntax highlighting style
% Use command `pygmentize -L styles` -- minted package
\newcommand{\PRSyntaxHighlightStyle}[0]{
tango
}
