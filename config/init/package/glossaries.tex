% enable the automatic creation of glossaries
\usepackage[acronym]{glossaries}
\makeglossaries

% so that we don't have to \protect the \gls function inside paragraph function arguments
\input{config/init/package/etoolbox.tex} % robustify is defined in etoolbox
\robustify{\gls}
